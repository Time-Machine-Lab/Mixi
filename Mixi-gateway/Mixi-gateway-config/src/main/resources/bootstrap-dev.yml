cloud:
  nacos:
    server-addr:
    username:
    password:
    discovery:
      namespace:
      cluster-name:
    config:
      file-extension:
      namespace:
      extension-configs:
        - dataId:
          refresh:
          group:
      shared-configs:
        - dataId:
          refresh:
          group:
  gateway:
    routes:

